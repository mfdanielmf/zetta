services:
  api:
    build: .
    container_name: backend-dev
    ports:
      - "8080:8080"

    volumes:
      - ./app:/app/app

    #Override config prod
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8080
      --reload
